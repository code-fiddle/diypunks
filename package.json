{
  "name": "diypunks",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ urbanisierung/diypunks",
    "create-local": "graph create --node http://localhost:8020/ urbanisierung/diypunks",
    "remove-local": "graph remove --node http://localhost:8020/ urbanisierung/diypunks",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 urbanisierung/diypunks"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.0",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
